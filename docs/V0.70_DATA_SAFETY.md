# v0.70 Data Safety

The receipt-reprint ledger database paging change is read-only.

It does not add DELETE or UPDATE operations for sales, sale receipt reprint audit rows, print jobs, tax snapshots, payments, journals, sync state, Drive data, or outbox history.

Recovery operations remain centralized in the unified print queue.
