# v0.80 Release Notes

## つぐレジ 0.80.0-dev.1

### レスポンシブ点検・精算履歴へ整合確認を接続

- レジ管理ハブから開く `SCR-520 点検・精算履歴` で「保存値と現在DBを照合」を実行可能にする。
- v0.78の整合確認ロジックを再利用し、保存snapshotと対象営業セッションの現在DB集計を比較する。
- v0.79の追記専用監査へ `OK / INFO / ALERT` を記録する。
- 実行時にログインセッションと `VIEW_SALES + X/Z対象権限` を再確認する。
- 監査記録失敗時は成功扱いにしない。
- 旧管理画面側の整合確認機能も維持する。
- 売上・税・支払・点検精算snapshot・既存監査を更新/削除しない。

### バージョン

- つぐレジ: `0.80.0-dev.1` / code `110`
- つぐレジ＋: `0.14.0-dev.1` / code `14`（変更なし）
- つぐレジ CD: `0.14.0-dev.1` / code `7`（変更なし）

### 実機確認

実機未確認。
