# つぐレジ v0.30.0-dev.1 リリースノート

## バージョン

- versionName: `0.30.0-dev.1`
- versionCode: `60`
- ベース: `develop/v0.29`
- PR: Draft、develop/v0.29向け、未マージ

## 重要修正

営業開始前の販売画面で、Z精算権限を持つ担当者にも`営業開始・状態画面へ`が表示されない不具合を修正しました。

原因は、点検・精算権限を`X_INSPECTION`と`Z_SETTLEMENT`へ分割した後も、`RegisterApplication.kt`の一部が旧互換権限`SETTLEMENT`を直接参照していたことです。

## 権限判定

- 営業開始・Z精算: `Z_SETTLEMENT`
- X点検: `X_INSPECTION`
- 旧`SETTLEMENT`: 既存保存データの互換展開だけに使用
- 新規UI表示条件・Activity入口・保存認可では旧権限を直接参照しない

追加した共通ポリシー:

- `BusinessStartNavigationPolicyV030`
- `ManagementNavigationPolicyV030`
- `OperationsAccessPolicyV030`
- `OperationsNavigationContractV030`

## 営業開始画面

販売画面の営業開始ゲートから、`BusinessStartActivityV030`へ直接遷移します。

- 管理メニュー経由を廃止
- Compact／Medium／Expandedの共通レスポンシブ値を利用
- 小さい画面・大きいフォントでは縦積み＋スクロール
- 営業開始入力、直近セッション集計、営業セッション履歴を表示
- 画面表示時にZ精算権限を確認
- 保存直前にも`SecureOperationsCoordinator`がZ精算権限を再確認

## 回帰テスト

`V030BusinessStartNavigationTest`へ次を追加しました。

- SALES＋Z_SETTLEMENTで営業開始導線を表示
- SALES＋X_INSPECTIONだけでは営業開始導線を非表示
- SALES＋SETTINGSだけでは営業開始導線を非表示
- 旧SETTLEMENT保存データを互換展開後に表示
- 管理導線へX_INSPECTION／Z_SETTLEMENTを含める
- RegisterApplicationが旧SETTLEMENTを直接参照しない
- 明示的な営業開始ルート値を使用

## CI・成果物

GitHub Actionsはv0.14～v0.30の累積テスト、Kotlinコンパイル、両APKビルド、SHA-256生成を行います。

成果物名:

- `TSUGUREGI_v0.30.0_dev1_business_start_responsive_debug.apk`
- `TSUGUREGI_CD_v0.13.0_dev6_transition_fix_debug.apk`

## 実機確認が必要な項目

- 権限ケースA～Dの表示
- 営業開始画面への直接遷移
- 営業開始後に販売画面のゲートが消えること
- 800×480相当で全操作へスクロール到達できること
- 1920×1080相当で主要領域が拡大されること
- Android表示サイズ・フォント倍率変更時の文字欠け、重なり、操作不能

上記は実機確認を行うまで確認済みとは扱いません。
