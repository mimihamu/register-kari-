name: Apply v0.48 settings entry

on:
  push:
    branches:
      - develop/v0.48
    paths:
      - tools/v048_apply.py
      - .github/workflows/v048-apply-temp.yml
  pull_request:
    branches:
      - develop/v0.47
    types:
      - opened
      - reopened
      - synchronize

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'develop/v0.48' || github.ref_name == 'develop/v0.48'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout v0.48
        uses: actions/checkout@v4
        with:
          ref: develop/v0.48
          fetch-depth: 0

      - name: Apply navigation and version changes
        run: python3 tools/v048_apply.py

      - name: Remove temporary patch files
        run: git rm tools/v048_apply.py .github/workflows/v048-apply-temp.yml

      - name: Commit and push v0.48
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "v0.48 expose Google Drive sync settings"
          git push origin HEAD:develop/v0.48
