name: v1.36 barcode integration runner

on:
  push:
    branches:
      - develop/v1.36
    paths:
      - .github/workflows/v136-barcode-patch.yml

permissions:
  contents: write

jobs:
  patch-and-test:
    runs-on: ubuntu-22.04
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with:
          ref: develop/v1.36
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Apply barcode integration
        run: python3 .github/scripts/v136_barcode_apply.py

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android API 36
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle 9.5
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'

      - name: Run barcode tests and Kotlin compile
        shell: bash
        run: |
          gradle :app:testDebugUnitTest \
            --tests 'jp.co.tenposinfo.register.V136BarcodeInputSafetyTest' \
            --tests 'jp.co.tenposinfo.register.V135ProductEntrySearchTest' \
            --no-daemon --stacktrace
          gradle :app:compileDebugKotlin --no-daemon --stacktrace

      - name: Commit product integration
        shell: bash
        run: |
          git config user.name 'mimihamu'
          git config user.email '74812001+mimihamu@users.noreply.github.com'
          git add \
            app/src/main/java/jp/co/tenposinfo/register/MainActivity.kt \
            app/src/main/java/jp/co/tenposinfo/register/CatalogHubActivityV030.kt \
            app/src/main/java/jp/co/tenposinfo/register/CatalogSettingsActivity.kt
          git diff --cached --check
          git commit -m 'feat(v1.36): complete HID barcode registration flow'
          git push origin HEAD:develop/v1.36
