name: COR-004 patch helper

on:
  pull_request:

jobs:
  validate-helper:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate helper execution
        run: echo "COR-004 patch helper is active"
