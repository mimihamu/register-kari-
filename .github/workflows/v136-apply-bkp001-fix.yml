name: v1.36 apply BKP-001 formal-spec fix

on:
  push:
    branches:
      - develop/v1.36
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout current branch
        uses: actions/checkout@v4
        with:
          ref: develop/v1.36
          fetch-depth: 0

      - name: Apply verified BKP-001 fix
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          def replace_once(text: str, old: str, new: str, label: str) -> str:
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{label}: expected exactly one match, found {count}')
              return text.replace(old, new, 1)

          # AutoBackupSettings.kt: preserve legacy fields/APIs, but make BKP-001 mandatory
          # triggers authoritative and add the formal 7..365 generation retention setting.
          p = Path('app/src/main/java/jp/co/tenposinfo/register/AutoBackupSettings.kt')
          s = p.read_text()
          s = replace_once(
              s,
              'const val DEFAULT_PERIODIC_BACKUP_WEEKDAY = 1 // ISO-8601 Monday=1 ... Sunday=7\n',
              'const val DEFAULT_PERIODIC_BACKUP_WEEKDAY = 1 // ISO-8601 Monday=1 ... Sunday=7\n'
              'const val DEFAULT_BACKUP_RETENTION_GENERATIONS = 30\n'
              'const val MIN_BACKUP_RETENTION_GENERATIONS = 7\n'
              'const val MAX_BACKUP_RETENTION_GENERATIONS = 365\n',
              'retention constants',
          )
          s = replace_once(
              s,
              '    val preferredHour: Int = DEFAULT_PERIODIC_BACKUP_HOUR,\n'
              '    val zRetentionBusinessDays: Int = DEFAULT_Z_BACKUP_BUSINESS_DAYS,\n',
              '    val preferredHour: Int = DEFAULT_PERIODIC_BACKUP_HOUR,\n'
              '    val retentionGenerations: Int = DEFAULT_BACKUP_RETENTION_GENERATIONS,\n'
              '    val zRetentionBusinessDays: Int = DEFAULT_Z_BACKUP_BUSINESS_DAYS,\n',
              'settings retention field',
          )
          old_sanitized = '''    fun sanitized(settings: AutoBackupSettings): AutoBackupSettings = settings.copy(
        preferredHour = settings.preferredHour.coerceIn(0, 23),
        preferredWeekday = settings.preferredWeekday.coerceIn(1, 7),
        zRetentionBusinessDays = settings.zRetentionBusinessDays.coerceIn(
            MIN_Z_BACKUP_BUSINESS_DAYS,
            MAX_Z_BACKUP_BUSINESS_DAYS,
        ),
        monthlyRetentionMonths = settings.monthlyRetentionMonths.coerceIn(
            MIN_MONTHLY_BACKUP_MONTHS,
            MAX_MONTHLY_BACKUP_MONTHS,
        ),
    )
'''
          new_sanitized = '''    fun sanitized(settings: AutoBackupSettings): AutoBackupSettings = settings.copy(
        // BKP-001 MUST: daily and post-settlement automatic backups cannot be disabled.
        periodicEnabled = true,
        cadence = PeriodicBackupCadence.DAILY,
        settlementAutoBackupEnabled = true,
        preferredHour = settings.preferredHour.coerceIn(0, 23),
        preferredWeekday = settings.preferredWeekday.coerceIn(1, 7),
        retentionGenerations = settings.retentionGenerations.coerceIn(
            MIN_BACKUP_RETENTION_GENERATIONS,
            MAX_BACKUP_RETENTION_GENERATIONS,
        ),
        // Legacy retention settings remain readable for migration/source compatibility only.
        zRetentionBusinessDays = settings.zRetentionBusinessDays.coerceIn(
            MIN_Z_BACKUP_BUSINESS_DAYS,
            MAX_Z_BACKUP_BUSINESS_DAYS,
        ),
        monthlyRetentionMonths = settings.monthlyRetentionMonths.coerceIn(
            MIN_MONTHLY_BACKUP_MONTHS,
            MAX_MONTHLY_BACKUP_MONTHS,
        ),
    )
'''
          s = replace_once(s, old_sanitized, new_sanitized, 'sanitized policy')
          s = replace_once(
              s,
              '        require(settings.preferredWeekday in 1..7) { "指定曜日が不正です" }\n',
              '        require(settings.preferredWeekday in 1..7) { "指定曜日が不正です" }\n'
              '        require(settings.retentionGenerations in MIN_BACKUP_RETENTION_GENERATIONS..MAX_BACKUP_RETENTION_GENERATIONS) {\n'
              '            "バックアップ保持世代は${MIN_BACKUP_RETENTION_GENERATIONS}～${MAX_BACKUP_RETENTION_GENERATIONS}世代で指定してください"\n'
              '        }\n',
              'retention validation',
          )
          s = replace_once(s, '        return settings\n    }\n\n    fun nextRunMillis(', '        return sanitized(settings)\n    }\n\n    fun nextRunMillis(', 'validated return')
          s = replace_once(
              s,
              '            preferredHour = preferences.getInt("preferred_hour", DEFAULT_PERIODIC_BACKUP_HOUR),\n'
              '            zRetentionBusinessDays = preferences.getInt("z_retention_business_days", DEFAULT_Z_BACKUP_BUSINESS_DAYS),\n',
              '            preferredHour = preferences.getInt("preferred_hour", DEFAULT_PERIODIC_BACKUP_HOUR),\n'
              '            retentionGenerations = preferences.getInt("backup.retention", DEFAULT_BACKUP_RETENTION_GENERATIONS),\n'
              '            zRetentionBusinessDays = preferences.getInt("z_retention_business_days", DEFAULT_Z_BACKUP_BUSINESS_DAYS),\n',
              'retention load',
          )
          s = replace_once(
              s,
              '            .putInt("preferred_hour", validated.preferredHour)\n'
              '            .putInt("z_retention_business_days", validated.zRetentionBusinessDays)\n',
              '            .putInt("preferred_hour", validated.preferredHour)\n'
              '            .putInt("backup.retention", validated.retentionGenerations)\n'
              '            .putInt("z_retention_business_days", validated.zRetentionBusinessDays)\n',
              'retention save',
          )
          disabled_block = '''        if (!settings.periodicEnabled) {
            workManager.cancelUniqueWork(UNIQUE_WORK_NAME)
            AutoBackupStatusStore(appContext).scheduled(null)
            return AutoBackupScheduleApplyResult(false, null, settings.cadence)
        }

'''
          s = replace_once(s, disabled_block, '', 'mandatory daily scheduler')
          s = replace_once(
              s,
              '            settings.cadence.intervalDays.toLong(),\n            TimeUnit.DAYS,\n',
              '            PeriodicBackupCadence.DAILY.intervalDays.toLong(),\n            TimeUnit.DAYS,\n',
              'daily work interval',
          )
          s = replace_once(
              s,
              '        if (!settings.periodicEnabled) return null\n        return AutoBackupSettingsPolicy.nextRunMillis(\n',
              '        return AutoBackupSettingsPolicy.nextRunMillis(\n',
              'next execution mandatory',
          )
          p.write_text(s)

          # AutoBackup.kt: production retention is now a total automatic-generation limit;
          # low-storage cleanup can go below that limit by deleting oldest successful auto generations.
          p = Path('app/src/main/java/jp/co/tenposinfo/register/AutoBackup.kt')
          s = p.read_text()
          insert_marker = '    private fun monthKey(createdAt: Long): String {'
          generation_policy = '''    fun selectGenerationDeletionCandidates(
        entries: List<BackupRetentionEntry>,
        generations: Int = DEFAULT_BACKUP_RETENTION_GENERATIONS,
    ): Set<String> {
        require(generations in MIN_BACKUP_RETENTION_GENERATIONS..MAX_BACKUP_RETENTION_GENERATIONS) {
            "バックアップ保持世代は${MIN_BACKUP_RETENTION_GENERATIONS}～${MAX_BACKUP_RETENTION_GENERATIONS}世代で指定してください"
        }
        val automaticReady = entries
            .filter {
                it.valid &&
                    !it.pendingRestore &&
                    it.state == AutoBackupFileState.READY &&
                    (it.reason == BackupCreationReason.Z_SETTLEMENT || it.reason == BackupCreationReason.PERIODIC)
            }
            .sortedByDescending { it.createdAt }
        return automaticReady.drop(generations).mapTo(linkedSetOf()) { it.fileName }
    }

    fun selectCapacityDeletionCandidates(entries: List<BackupRetentionEntry>): List<String> {
        val automaticReady = entries
            .filter {
                it.valid &&
                    !it.pendingRestore &&
                    it.state == AutoBackupFileState.READY &&
                    (it.reason == BackupCreationReason.Z_SETTLEMENT || it.reason == BackupCreationReason.PERIODIC)
            }
            .sortedBy { it.createdAt }
        // Always retain the newest successful automatic generation even under capacity pressure.
        return automaticReady.dropLast(1).map { it.fileName }
    }

'''
          if insert_marker not in s:
              raise SystemExit('generation policy insertion marker missing')
          s = s.replace(insert_marker, generation_policy + insert_marker, 1)

          class_start = s.index('class AutoBackupRetentionManager(context: Context) {')
          class_end = s.index('object AutoBackupScheduler {', class_start)
          new_manager = '''class AutoBackupRetentionManager(context: Context) {
    private val appContext = context.applicationContext
    private val backupDir = File(appContext.filesDir, "data_backups")
    private val manager = DataProtectionManager(appContext)
    private val metadataStore = AutoBackupMetadataStore(appContext)

    fun apply(actorName: String, capacityReliefDatabaseBytes: Long? = null): BackupRetentionResult {
        val backups = manager.listBackups()
        val metadata = metadataStore.readAll()
        val pendingName = manager.pendingRestoreStatus().backupFileName
        val entries = backups.map { backup ->
            val meta = metadata[backup.fileName]
            BackupRetentionEntry(
                fileName = backup.fileName,
                createdAt = backup.createdAt,
                valid = backup.valid,
                reason = meta?.reason,
                businessDate = meta?.businessDate,
                state = meta?.state ?: AutoBackupFileState.READY,
                pendingRestore = backup.fileName == pendingName,
            )
        }
        val settings = AutoBackupSettingsStore(appContext).load()
        val planned = linkedSetOf<String>()
        planned += AutoBackupRetentionPolicy.selectGenerationDeletionCandidates(
            entries = entries,
            generations = settings.retentionGenerations,
        ).sortedBy { fileName -> entries.first { it.fileName == fileName }.createdAt }

        AutoBackupAudit.record(
            appContext,
            "DATA_BACKUP_RETENTION_STARTED",
            "保持=${settings.retentionGenerations}世代 / 初期削除候補 ${planned.size}件 / capacityRelief=${capacityReliefDatabaseBytes != null}",
            actorName,
        )
        val deleted = mutableListOf<String>()
        val failed = linkedMapOf<String, String>()

        fun deleteOne(fileName: String) {
            val archive = File(backupDir, BackupFilePolicy.requireSafe(fileName))
            runCatching {
                require(archive.isFile) { "対象ファイルがありません" }
                require(archive.delete()) { "バックアップを削除できません" }
                metadataStore.delete(fileName)
                deleted += fileName
                AutoBackupAudit.record(appContext, "DATA_BACKUP_RETENTION_DELETED", fileName, actorName)
            }.onFailure { error ->
                failed[fileName] = error.message ?: error.javaClass.simpleName
                runCatching {
                    AutoBackupAudit.record(appContext, "DATA_BACKUP_RETENTION_FAILED", "$fileName / ${failed[fileName]}", actorName)
                }
            }
        }

        planned.toList().forEach(::deleteOne)

        if (capacityReliefDatabaseBytes != null &&
            !AutoBackupStoragePolicy.hasCapacity(availableBytes(), capacityReliefDatabaseBytes)
        ) {
            val remaining = entries.filter { it.fileName !in deleted }
            val capacityCandidates = AutoBackupRetentionPolicy.selectCapacityDeletionCandidates(remaining)
            for (fileName in capacityCandidates) {
                if (AutoBackupStoragePolicy.hasCapacity(availableBytes(), capacityReliefDatabaseBytes)) break
                if (fileName !in planned) planned += fileName
                deleteOne(fileName)
            }
        }
        return BackupRetentionResult(planned.toList(), deleted, failed)
    }

    private fun availableBytes(): Long = StatFs(appContext.filesDir.absolutePath).availableBytes
}

'''
          s = s[:class_start] + new_manager + s[class_end:]

          z_start = s.index('        if (!AutoBackupSettingsStore(appContext).load().settlementAutoBackupEnabled) {', s.index('fun enqueueZSettlement('))
          z_end = s.index('        enqueue(', z_start)
          s = s[:z_start] + s[z_end:]

          old_low_storage = '''            if (!AutoBackupStoragePolicy.hasCapacity(availableBytes(appContext), databaseBytes)) {
                val retention = AutoBackupRetentionManager(appContext).apply(actorName)
                statusStore.retention(retention.summary())
            }
'''
          new_low_storage = '''            if (!AutoBackupStoragePolicy.hasCapacity(availableBytes(appContext), databaseBytes)) {
                val retention = AutoBackupRetentionManager(appContext).apply(
                    actorName,
                    capacityReliefDatabaseBytes = databaseBytes,
                )
                statusStore.retention(retention.summary())
            }
'''
          s = replace_once(s, old_low_storage, new_low_storage, 'capacity retention invocation')
          p.write_text(s)

          # Settings UI: mandatory triggers are no longer presented as disableable controls.
          p = Path('app/src/main/java/jp/co/tenposinfo/register/AutoBackupSettingsActivity.kt')
          s = p.read_text()
          s = replace_once(
              s,
              '    var monthlyRetentionText by remember { mutableStateOf(initial.monthlyRetentionMonths.toString()) }\n',
              '    var monthlyRetentionText by remember { mutableStateOf(initial.monthlyRetentionMonths.toString()) }\n'
              '    var retentionGenerationsText by remember { mutableStateOf(initial.retentionGenerations.toString()) }\n',
              'ui retention state',
          )
          s = replace_once(
              s,
              '        val months = monthlyRetentionText.toIntOrNull()\n',
              '        val months = monthlyRetentionText.toIntOrNull()\n'
              '        val retentionGenerations = retentionGenerationsText.toIntOrNull()\n',
              'ui retention parse',
          )
          s = replace_once(
              s,
              '                    periodicEnabled = periodicEnabled,\n                    cadence = cadence,\n                    preferredHour = hour ?: error("実行時刻を入力してください"),\n',
              '                    periodicEnabled = true,\n                    cadence = PeriodicBackupCadence.DAILY,\n                    preferredHour = hour ?: error("実行時刻を入力してください"),\n'
              '                    retentionGenerations = retentionGenerations ?: error("保持世代を入力してください"),\n',
              'ui mandatory schedule draft',
          )
          s = replace_once(
              s,
              '                    settlementAutoBackupEnabled = settlementAutoBackupEnabled,\n',
              '                    settlementAutoBackupEnabled = true,\n',
              'ui mandatory settlement draft',
          )
          s = replace_once(s, 'Text("定期スケジュール・Z精算後を個別設定", color = Color.White)', 'Text("毎日＋Z精算後は常時有効", color = Color.White)', 'ui header')
          old_switch = '''                            Row(verticalAlignment = Alignment.CenterVertically, horizontalArrangement = Arrangement.spacedBy(12.dp)) {
                                Switch(checked = periodicEnabled, onCheckedChange = { periodicEnabled = it; message = null })
                                Text(if (periodicEnabled) "定期バックアップ：ON" else "定期バックアップ：OFF", fontWeight = FontWeight.Bold)
                            }
'''
          new_switch = '''                            Text("1日1回の暗号化バックアップは常時有効", color = AbsGreen, fontWeight = FontWeight.Bold)
                            Text("Z精算後バックアップは常時有効", color = AbsGreen, fontWeight = FontWeight.Bold)
'''
          s = replace_once(s, old_switch, new_switch, 'ui mandatory labels')

          controls_start = s.index('                            Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(10.dp)) {')
          controls_end = s.index('                            OutlinedTextField(', controls_start)
          s = s[:controls_start] + '''                            Text(
                                "BKP-001に従い、毎日の定期バックアップとZ精算確定後バックアップは停止できません。指定曜日設定は互換情報として保持しますが、毎日バックアップを置き換えません。",
                                color = Color.DarkGray,
                            )
''' + s[controls_end:]
          s = s.replace('                                enabled = periodicEnabled,\n', '', 1)
          s = s.replace('if (preview == null) "次回予定：定期バックアップOFF" else "次回予定（目安）：${formatBackupSchedule(preview)}"', '"次回予定（目安）：${formatBackupSchedule(preview ?: System.currentTimeMillis())}"', 1)
          s = s.replace('color = if (preview == null) Color.DarkGray else AbsGreen,', 'color = AbsGreen,', 1)

          retention_start = s.index('                            Text("保存世代",')
          next_card = s.index('                    Card(colors = CardDefaults.cardColors(containerColor = Color.White)', retention_start + 1)
          retention_replacement = '''                            Text("保存世代", color = AbsNavy, fontSize = 22.sp, fontWeight = FontWeight.Bold)
                            Text("自動バックアップは7～365世代で保持し、初期値は30世代です。容量閾値を超えた場合は古い成功世代から削除します。手動作成・復元予約中は保護します。")
                            OutlinedTextField(
                                value = retentionGenerationsText,
                                onValueChange = { retentionGenerationsText = it.filter(Char::isDigit).take(3); message = null },
                                label = { Text("バックアップ保持世代（7～365）") },
                                keyboardOptions = KeyboardOptions(keyboardType = KeyboardType.Number),
                                singleLine = true,
                                modifier = Modifier.fillMaxWidth(),
                            )
                        }
                    }

'''
          s = s[:retention_start] + retention_replacement + s[next_card:]
          s = replace_once(
              s,
              ' / settlementAutoBackup=${saved.settlementAutoBackupEnabled} / zDays=${saved.zRetentionBusinessDays} / months=${saved.monthlyRetentionMonths} / notify=${saved.failureNotificationsEnabled}',
              ' / settlementAutoBackup=${saved.settlementAutoBackupEnabled} / retention=${saved.retentionGenerations} / zDays=${saved.zRetentionBusinessDays} / months=${saved.monthlyRetentionMonths} / notify=${saved.failureNotificationsEnabled}',
              'ui audit retention',
          )
          old_message = '''                        message = if (schedule.nextScheduledAt == null) {
                            "保存しました。定期バックアップはOFFです。"
                        } else {
                            "保存しました。次回予定 ${formatBackupSchedule(schedule.nextScheduledAt)}"
                        }
'''
          new_message = '''                        message = schedule.nextScheduledAt?.let {
                            "保存しました。次回予定 ${formatBackupSchedule(it)}"
                        } ?: "保存しました。毎日バックアップを再登録しました。"
'''
          s = replace_once(s, old_message, new_message, 'ui save message')
          p.write_text(s)

          # Replace v1.39 schedule tests with the formal BKP-001 contract plus migration coverage.
          Path('app/src/test/java/jp/co/tenposinfo/register/V139AutoBackupScheduleTest.kt').write_text(r'''package jp.co.tenposinfo.register

import java.io.File
import java.time.ZoneId
import java.time.ZonedDateTime
import org.junit.Assert.assertEquals
import org.junit.Assert.assertFalse
import org.junit.Assert.assertThrows
import org.junit.Assert.assertTrue
import org.junit.Test

class V139AutoBackupScheduleTest {
    private val zone = ZoneId.of("Asia/Tokyo")

    @Test
    fun dailyScheduleUsesNextConfiguredHour() {
        val before = ZonedDateTime.of(2026, 8, 24, 2, 0, 0, 0, zone)
        val after = ZonedDateTime.of(2026, 8, 24, 4, 0, 0, 0, zone)
        assertEquals(
            ZonedDateTime.of(2026, 8, 24, 3, 0, 0, 0, zone).toInstant().toEpochMilli(),
            AutoBackupSettingsPolicy.nextRunMillis(before.toInstant().toEpochMilli(), 3, zone),
        )
        assertEquals(
            ZonedDateTime.of(2026, 8, 25, 3, 0, 0, 0, zone).toInstant().toEpochMilli(),
            AutoBackupSettingsPolicy.nextRunMillis(after.toInstant().toEpochMilli(), 3, zone),
        )
    }

    @Test
    fun legacyDisabledOrWeeklySettingsAreMigratedToMandatoryDailyAndSettlement() {
        val migrated = AutoBackupSettingsPolicy.sanitized(
            AutoBackupSettings(
                periodicEnabled = false,
                cadence = PeriodicBackupCadence.WEEKLY,
                preferredWeekday = 5,
                settlementAutoBackupEnabled = false,
            ),
        )
        assertTrue(migrated.periodicEnabled)
        assertEquals(PeriodicBackupCadence.DAILY, migrated.cadence)
        assertTrue(migrated.settlementAutoBackupEnabled)
        assertEquals(5, migrated.preferredWeekday)
    }

    @Test
    fun formalRetentionDefaultsToThirtyAndRejectsOutsideSevenTo365() {
        assertEquals(30, AutoBackupSettings().retentionGenerations)
        assertThrows(IllegalArgumentException::class.java) {
            AutoBackupSettingsPolicy.validated(AutoBackupSettings(retentionGenerations = 6))
        }
        assertThrows(IllegalArgumentException::class.java) {
            AutoBackupSettingsPolicy.validated(AutoBackupSettings(retentionGenerations = 366))
        }
        assertEquals(
            7,
            AutoBackupSettingsPolicy.sanitized(AutoBackupSettings(retentionGenerations = 1)).retentionGenerations,
        )
        assertEquals(
            365,
            AutoBackupSettingsPolicy.sanitized(AutoBackupSettings(retentionGenerations = 999)).retentionGenerations,
        )
    }

    @Test
    fun generationRetentionDeletesOldestSuccessfulAutomaticBackupsOnly() {
        val automatic = (1L..10L).map { index ->
            BackupRetentionEntry(
                fileName = "auto-$index.tgbak",
                createdAt = index,
                valid = true,
                reason = if (index % 2L == 0L) BackupCreationReason.PERIODIC else BackupCreationReason.Z_SETTLEMENT,
                businessDate = "2026-08-${index.toString().padStart(2, '0')}",
                state = AutoBackupFileState.READY,
                pendingRestore = false,
            )
        }
        val manual = BackupRetentionEntry(
            fileName = "manual.tgbak",
            createdAt = 0L,
            valid = true,
            reason = BackupCreationReason.MANUAL,
            businessDate = null,
            state = AutoBackupFileState.READY,
            pendingRestore = false,
        )
        val deletion = AutoBackupRetentionPolicy.selectGenerationDeletionCandidates(
            entries = automatic + manual,
            generations = 7,
        )
        assertEquals(setOf("auto-1.tgbak", "auto-2.tgbak", "auto-3.tgbak"), deletion)
        assertFalse("manual.tgbak" in deletion)
    }

    @Test
    fun capacityReliefNeverSelectsNewestSuccessfulAutomaticGeneration() {
        val entries = (1L..3L).map { index ->
            BackupRetentionEntry(
                fileName = "auto-$index.tgbak",
                createdAt = index,
                valid = true,
                reason = BackupCreationReason.PERIODIC,
                businessDate = null,
                state = AutoBackupFileState.READY,
                pendingRestore = false,
            )
        }
        assertEquals(
            listOf("auto-1.tgbak", "auto-2.tgbak"),
            AutoBackupRetentionPolicy.selectCapacityDeletionCandidates(entries),
        )
    }

    @Test
    fun productSourcesEnforceFormalBkp001Contract() {
        val mainRoot = File("src/main/java/jp/co/tenposinfo/register")
        val worker = File(mainRoot, "AutoBackup.kt").readText()
        val settings = File(mainRoot, "AutoBackupSettings.kt").readText()
        val ui = File(mainRoot, "AutoBackupSettingsActivity.kt").readText()

        assertTrue(settings.contains("DEFAULT_BACKUP_RETENTION_GENERATIONS = 30"))
        assertTrue(settings.contains("MIN_BACKUP_RETENTION_GENERATIONS = 7"))
        assertTrue(settings.contains("MAX_BACKUP_RETENTION_GENERATIONS = 365"))
        assertTrue(settings.contains("\"backup.retention\""))
        assertTrue(settings.contains("periodicEnabled = true"))
        assertTrue(settings.contains("cadence = PeriodicBackupCadence.DAILY"))
        assertTrue(settings.contains("settlementAutoBackupEnabled = true"))
        assertTrue(worker.contains("selectGenerationDeletionCandidates"))
        assertTrue(worker.contains("selectCapacityDeletionCandidates"))
        assertTrue(worker.contains("capacityReliefDatabaseBytes"))
        assertTrue(worker.contains("DataProtectionManager(appContext)"))
        assertTrue(worker.contains("manager.createBackup(actorName)"))
        val zMethod = worker.substringAfter("fun enqueueZSettlement(").substringBefore("fun enqueueManualNow(")
        assertFalse(zMethod.contains("DATA_BACKUP_Z_SKIPPED_DISABLED"))
        assertTrue(zMethod.contains("uniqueZWorkName(settlementId)"))
        assertTrue(ui.contains("1日1回の暗号化バックアップは常時有効"))
        assertTrue(ui.contains("Z精算後バックアップは常時有効"))
        assertTrue(ui.contains("バックアップ保持世代（7～365）"))
    }
}
''')

          # Sanity-check exact formal markers before committing.
          settings = Path('app/src/main/java/jp/co/tenposinfo/register/AutoBackupSettings.kt').read_text()
          worker = Path('app/src/main/java/jp/co/tenposinfo/register/AutoBackup.kt').read_text()
          ui = Path('app/src/main/java/jp/co/tenposinfo/register/AutoBackupSettingsActivity.kt').read_text()
          assert 'DEFAULT_BACKUP_RETENTION_GENERATIONS = 30' in settings
          assert '"backup.retention"' in settings
          assert 'Z精算後バックアップは常時有効' in ui
          assert 'selectGenerationDeletionCandidates' in worker
          assert 'DATA_BACKUP_Z_SKIPPED_DISABLED' not in worker[worker.index('fun enqueueZSettlement('):worker.index('fun enqueueManualNow(')]
          PY

      - name: Remove temporary diagnostic and patch workflows
        shell: bash
        run: |
          rm -f .github/workflows/v136-unit-test-diagnostic.yml
          rm -f .github/workflows/v136-apply-bkp001-fix.yml

      - name: Commit clean product changes
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin develop/v1.36
          test "$(git rev-parse HEAD)" = "$(git rev-parse origin/develop/v1.36)"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "fix(v1.36): align automatic backup with BKP-001"
          git push origin HEAD:develop/v1.36
