name: Apply v0.37 UI fix

on:
  push:
    branches:
      - develop/v0.37
    paths:
      - tools/v037_fix_ui.py
      - .github/workflows/v037-fix.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout v0.37
        uses: actions/checkout@v4
        with:
          ref: develop/v0.37
          fetch-depth: 0

      - name: Apply UI fix
        run: python3 tools/v037_fix_ui.py

      - name: Commit UI fix
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add customer-display/src/main/java/jp/co/tenposinfo/register/cd/MainActivity.kt
          git diff --cached --check
          git commit -m "fix: complete customer display density integration"
          git push origin HEAD:develop/v0.37
